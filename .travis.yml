language: python

python:
  - 3.5

cache: pip

install:
  - pip install -q -r requirements.txt
  - pip install -q pylint

script:
  - python3 -m pylint **/*.py --indent-string="  " --max-line-length=80
  - bash run_tests.sh
